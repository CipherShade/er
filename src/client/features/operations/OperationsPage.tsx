import { useEffect, useState, type FormEvent } from 'react';
import { BarChart3, ClipboardCheck, Coins, Search, Wallet } from 'lucide-react';
import { useTranslation } from 'react-i18next';
import { io } from 'socket.io-client';
import { apiUrl, socketUrl } from '../../lib/config';
import i18n from '../../locales/i18n';
import { EmptyState, ErrorState, LoadingState } from '../../components/ui/AsyncState';

type Mode = 'lobby' | 'shift' | 'reconciliation' | 'settlement' | 'reports';
type Session = { id: string; title: string; startTime: string; sessionPrice: number; centerFeePerStudent: number; currentLobbyCount: number; teacher: { fullName: string }; room: { name: string; capacity: number } };
type Student = { id: string; fullName: string; studentCode: string };
type Shift = { id: string; financials?: { expectedCashInDrawer: number; totalCashCollected: number; totalTeacherCashPayouts: number; totalCashExpenses: number } };

async function request<T>(path: string, options?: RequestInit): Promise<T> {
  const response = await fetch(apiUrl(`/api${path}`), { ...options, credentials: 'include', headers: { 'Content-Type': 'application/json', ...options?.headers } });
  if (!response.ok) throw new Error('REQUEST_FAILED');
  return (await response.json() as { data: T }).data;
}

const money = (value: number | undefined) => new Intl.NumberFormat(i18n.language === 'ar' ? 'ar-EG' : 'en', { style: 'currency', currency: 'EGP' }).format(Number(value || 0));

export function OperationsPage({ mode }: { mode: Mode }) {
  if (mode === 'lobby') return <LobbyPage />;
  if (mode === 'shift') return <ShiftPage />;
  if (mode === 'reconciliation') return <SessionActionPage kind="reconciliation" />;
  if (mode === 'settlement') return <SessionActionPage kind="settlement" />;
  return <ReportsPage />;
}

function PageHeader({ icon: Icon, title, subtitle }: { icon: typeof Wallet; title: string; subtitle: string }) {
  return <header><div className="flex items-center gap-3 text-emerald-400"><Icon className="h-5 w-5" aria-hidden="true" /><span className="text-sm font-semibold">{subtitle}</span></div><h2 className="mt-2 text-2xl font-bold text-white">{title}</h2></header>;
}
function Notice({ error, success }: { error: string; success: string }) { return <>{error && <ErrorState message={error} />}{success && <p role="status" aria-live="polite" className="rounded-lg border border-emerald-500/30 bg-emerald-500/10 p-3 text-sm text-emerald-200">{success}</p>}</>; }

function LobbyPage() {
  const { t } = useTranslation();
  const [sessions, setSessions] = useState<Session[]>([]); const [students, setStudents] = useState<Student[]>([]); const [query, setQuery] = useState(''); const [selectedSession, setSelectedSession] = useState(''); const [paymentMethod, setPaymentMethod] = useState('CASH'); const [reference, setReference] = useState('');
  const [loading, setLoading] = useState(true); const [searching, setSearching] = useState(false); const [submittingId, setSubmittingId] = useState<string | null>(null); const [error, setError] = useState(''); const [success, setSuccess] = useState('');
  const load = async () => { setLoading(true); setError(''); try { setSessions((await request<{ sessions: Session[] }>('/attendances/sessions/active')).sessions); } catch { setError(t('operations.loadError')); } finally { setLoading(false); } };
  useEffect(() => { void load(); const socket = socketUrl ? io(socketUrl, { withCredentials: true }) : io({ withCredentials: true }); socket.on('connect', () => socket.emit('join:lobby')); socket.on('attendance:checked_in', () => void load()); return () => { socket.emit('leave:lobby'); socket.disconnect(); }; }, []);
  useEffect(() => { if (!query.trim()) { setStudents([]); setSearching(false); return; } const timer = window.setTimeout(() => { setSearching(true); void request<{ students: Student[] }>(`/registry/students?search=${encodeURIComponent(query)}`).then((data) => setStudents(data.students)).catch(() => setStudents([])).finally(() => setSearching(false)); }, 250); return () => window.clearTimeout(timer); }, [query]);
  async function checkIn(student: Student) { if (!selectedSession) { setError(t('operations.chooseSession')); return; } if (submittingId) return; setSubmittingId(student.id); setError(''); setSuccess(''); try { await request('/attendances/checkin', { method: 'POST', body: JSON.stringify({ sessionId: selectedSession, studentId: student.id, paymentMethod, paymentReference: reference || undefined }) }); setSuccess(t('operations.checkInSuccess')); setQuery(''); setReference(''); await load(); } catch { setError(t('operations.checkInError')); } finally { setSubmittingId(null); } }
  return <section className="space-y-6"><PageHeader icon={Search} title={t('operations.lobby.title')} subtitle={t('operations.lobby.subtitle')} /><Notice error={error} success={success} />{loading ? <LoadingState label={t('operations.loading')} /> : <div className="grid gap-4 xl:grid-cols-[1.15fr_0.85fr]"><div className="space-y-4"><div className="grid gap-3 sm:grid-cols-2">{sessions.map((session) => <button key={session.id} type="button" onClick={() => setSelectedSession(session.id)} aria-pressed={selectedSession === session.id} className={`min-h-32 rounded-xl border p-4 text-start focus-visible:outline focus-visible:outline-2 focus-visible:outline-emerald-400 ${selectedSession === session.id ? 'border-emerald-500 bg-emerald-500/10' : 'border-slate-800 bg-slate-900'}`}><div className="flex justify-between gap-3"><span className="font-bold text-white">{session.title}</span><span className="text-xs text-emerald-300">{session.currentLobbyCount}/{session.room.capacity}</span></div><p className="mt-2 text-sm text-slate-400">{session.teacher.fullName} · {session.room.name}</p><p className="mt-1 text-xs text-slate-500">{new Date(session.startTime).toLocaleTimeString()}</p></button>)}</div>{sessions.length === 0 && <EmptyState message={t('operations.lobby.empty')} />}</div><div className="rounded-xl border border-slate-800 bg-slate-900 p-5"><label className="text-sm font-semibold text-slate-300">{t('operations.lobby.studentSearch')}<div className="relative mt-2"><Search className="absolute start-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" aria-hidden="true" /><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} className="w-full rounded-lg border border-slate-700 bg-slate-950 py-3 ps-10 pe-3 text-white outline-none focus:border-emerald-500" placeholder={t('operations.lobby.searchPlaceholder')} /></div></label><div className="mt-3 space-y-2" aria-live="polite">{searching && <LoadingState />}{!searching && query && students.length === 0 && <EmptyState message={t('ui.noResults')} />}{students.map((student) => <div key={student.id} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-slate-800 p-3"><div><p className="font-semibold text-white">{student.fullName}</p><p className="text-xs text-slate-500">{student.studentCode}</p></div><button type="button" disabled={!selectedSession || submittingId !== null} onClick={() => void checkIn(student)} className="rounded-lg bg-emerald-600 px-3 py-2 text-xs font-bold disabled:cursor-wait disabled:opacity-60">{submittingId === student.id ? t('ui.processing') : t('actions.checkIn')}</button></div>)}</div><div className="mt-4 grid gap-3 sm:grid-cols-2"><select aria-label={t('operations.lobby.paymentMethod')} value={paymentMethod} onChange={(event) => setPaymentMethod(event.target.value)} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-3 text-white"><option value="CASH">{t('paymentMethods.cash')}</option><option value="VODAFONE_CASH">{t('paymentMethods.vodafoneCash')}</option><option value="INSTAPAY">{t('paymentMethods.instapay')}</option></select><input value={reference} onChange={(event) => setReference(event.target.value)} placeholder={t('operations.lobby.reference')} className="rounded-lg border border-slate-700 bg-slate-950 px-3 py-3 text-white" /></div></div></div>}</section>;
}

function ShiftPage() {
  const { t } = useTranslation(); const [shift, setShift] = useState<Shift | null>(null); const [desk, setDesk] = useState('Desk 1'); const [opening, setOpening] = useState('0'); const [actual, setActual] = useState('0'); const [notes, setNotes] = useState(''); const [expense, setExpense] = useState({ category: '', amount: '', description: '' }); const [loading, setLoading] = useState(true); const [pending, setPending] = useState(false); const [error, setError] = useState(''); const [success, setSuccess] = useState('');
  const load = async () => { setLoading(true); try { setShift((await request<{ shift: Shift | null }>('/shifts/current')).shift); } catch { setError(t('operations.loadError')); } finally { setLoading(false); } }; useEffect(() => { void load(); }, []);
  async function submit(path: string, body: object, message: string) { if (pending) return; setPending(true); setError(''); setSuccess(''); try { await request(path, { method: 'POST', body: JSON.stringify(body) }); setSuccess(message); await load(); } catch { setError(t('operations.saveError')); } finally { setPending(false); } }
  return <section className="space-y-6"><PageHeader icon={Wallet} title={t('operations.shift.title')} subtitle={t('operations.shift.subtitle')} /><Notice error={error} success={success} />{loading ? <LoadingState /> : !shift ? <form onSubmit={(event) => { event.preventDefault(); void submit('/shifts/open', { deskIdentifier: desk, openingCash: Number(opening) }, t('operations.shift.opened')); }} className="max-w-xl rounded-xl border border-slate-800 bg-slate-900 p-5"><Field label={t('operations.shift.desk')} value={desk} onChange={setDesk} /><Field label={t('operations.shift.openingCash')} value={opening} onChange={setOpening} type="number" /><button disabled={pending} className="mt-4 rounded-lg bg-emerald-600 px-4 py-2 font-bold disabled:opacity-60">{pending ? t('ui.processing') : t('actions.openShift')}</button></form> : <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[['expectedCashInDrawer', shift.financials?.expectedCashInDrawer], ['totalCashCollected', shift.financials?.totalCashCollected], ['totalTeacherCashPayouts', shift.financials?.totalTeacherCashPayouts], ['totalCashExpenses', shift.financials?.totalCashExpenses]].map(([key, value]) => <Metric key={key as string} label={t(`operations.shift.${key as string}`)} value={money(value as number)} />)}</div><form onSubmit={(event) => { event.preventDefault(); void submit('/shifts/expenses', { ...expense, amount: Number(expense.amount), paymentMethod: 'CASH' }, t('operations.shift.expenseSaved')); }} className="grid gap-3 rounded-xl border border-slate-800 bg-slate-900 p-5 sm:grid-cols-2 xl:grid-cols-4"><Field label={t('operations.shift.category')} value={expense.category} onChange={(value) => setExpense({ ...expense, category: value })} /><Field label={t('operations.shift.amount')} value={expense.amount} onChange={(value) => setExpense({ ...expense, amount: value })} type="number" /><Field label={t('operations.shift.description')} value={expense.description} onChange={(value) => setExpense({ ...expense, description: value })} /><button disabled={pending} className="self-end rounded-lg bg-amber-600 px-4 py-3 font-bold disabled:opacity-60">{pending ? t('ui.processing') : t('operations.shift.addExpense')}</button></form><form onSubmit={(event) => { event.preventDefault(); void submit('/shifts/close', { actualCashCounted: Number(actual), closingNotes: notes }, t('operations.shift.closed')); }} className="max-w-xl rounded-xl border border-slate-800 bg-slate-900 p-5"><Field label={t('operations.shift.actualCash')} value={actual} onChange={setActual} type="number" /><Field label={t('operations.shift.notes')} value={notes} onChange={setNotes} required={false} /><button disabled={pending} className="mt-4 rounded-lg bg-red-600 px-4 py-2 font-bold disabled:opacity-60">{pending ? t('ui.processing') : t('actions.closeShift')}</button></form></div>}</section>;
}

function SessionActionPage({ kind }: { kind: 'reconciliation' | 'settlement' }) {
  const { t } = useTranslation(); const [sessions, setSessions] = useState<Session[]>([]); const [sessionId, setSessionId] = useState(''); const [first, setFirst] = useState(''); const [second, setSecond] = useState(''); const [notes, setNotes] = useState(''); const [loading, setLoading] = useState(true); const [pending, setPending] = useState(false); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const selected = sessions.find((item) => item.id === sessionId);
  const load = async () => { setLoading(true); setError(''); try { const path = kind === 'reconciliation' ? '/attendances/sessions/active' : '/scheduling/sessions'; setSessions((await request<{ sessions: Session[] }>(path)).sessions); } catch { setError(t('operations.loadError')); } finally { setLoading(false); } }; useEffect(() => { void load(); }, [kind]);
  async function submit(event: FormEvent) { event.preventDefault(); if (!sessionId || pending) return; setPending(true); setError(''); try { const body = kind === 'reconciliation' ? { assistantCount: Number(first), reconciledHeadcount: Number(second), resolutionNotes: notes || undefined } : { payoutMethod: first, recipientName: second }; await request(`/sessions/${sessionId}/${kind === 'reconciliation' ? 'reconcile' : 'settle'}`, { method: 'POST', body: JSON.stringify(body) }); setSuccess(t(`operations.${kind}.saved`)); } catch { setError(t('operations.saveError')); } finally { setPending(false); } }
  const icon = kind === 'reconciliation' ? ClipboardCheck : Coins;
  return <section className="space-y-6"><PageHeader icon={icon} title={t(`operations.${kind}.title`)} subtitle={t(`operations.${kind}.subtitle`)} /><Notice error={error} success={success} />{loading ? <LoadingState /> : sessions.length === 0 ? <EmptyState message={t('operations.lobby.empty')} /> : <form onSubmit={submit} className="max-w-xl space-y-4 rounded-xl border border-slate-800 bg-slate-900 p-5"><Select label={t(`operations.${kind}.session`)} value={sessionId} onChange={setSessionId} options={sessions.map((item) => ({ value: item.id, label: kind === 'reconciliation' ? `${item.title} (${item.currentLobbyCount})` : item.title }))} />{kind === 'reconciliation' ? <><p className="text-sm text-slate-400">{t('operations.reconciliation.lobbyCount')}: {selected?.currentLobbyCount ?? '-'}</p><Field label={t('operations.reconciliation.assistantCount')} value={first} onChange={setFirst} type="number" /><Field label={t('operations.reconciliation.headcount')} value={second} onChange={setSecond} type="number" /><Field label={t('operations.reconciliation.notes')} value={notes} onChange={setNotes} required={false} /></> : <><div className="grid grid-cols-2 gap-3"><Metric label={t('operations.settlement.price')} value={money(selected?.sessionPrice)} /><Metric label={t('operations.settlement.centerFee')} value={money(selected?.centerFeePerStudent)} /></div><Select label={t('operations.settlement.payment')} value={first} onChange={setFirst} options={[{ value: 'CASH', label: t('paymentMethods.cash') }, { value: 'VODAFONE_CASH', label: t('paymentMethods.vodafoneCash') }, { value: 'INSTAPAY', label: t('paymentMethods.instapay') }]} /><Field label={t('operations.settlement.recipient')} value={second} onChange={setSecond} /></>}<button disabled={!sessionId || pending} className="rounded-lg bg-emerald-600 px-4 py-2 font-bold disabled:cursor-not-allowed disabled:opacity-50">{pending ? t('ui.processing') : t(kind === 'reconciliation' ? 'actions.reconcile' : 'actions.settlePayout')}</button></form>}</section>;
}

function ReportsPage() {
  const { t } = useTranslation(); const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); const [report, setReport] = useState<Record<string, unknown> | null>(null); const [shiftId, setShiftId] = useState(''); const [entries, setEntries] = useState<Record<string, unknown>[]>([]); const [reportLoading, setReportLoading] = useState(false); const [auditLoading, setAuditLoading] = useState(false); const [error, setError] = useState('');
  async function loadReport() { if (reportLoading) return; setReportLoading(true); setError(''); try { setReport(await request<Record<string, unknown>>(`/reports/daily?date=${date}`)); } catch { setError(t('operations.loadError')); } finally { setReportLoading(false); } }
  async function loadAudit() { if (!shiftId || auditLoading) return; setAuditLoading(true); setError(''); try { setEntries((await request<{ entries: Record<string, unknown>[] }>(`/reports/shifts/${shiftId}/audit`)).entries); } catch { setError(t('operations.loadError')); } finally { setAuditLoading(false); } }
  return <section className="space-y-6"><PageHeader icon={BarChart3} title={t('operations.reports.title')} subtitle={t('operations.reports.subtitle')} /><Notice error={error} success="" /><div className="flex flex-wrap gap-3"><label className="sr-only" htmlFor="report-date">{t('operations.reports.load')}</label><input id="report-date" type="date" value={date} onChange={(event) => setDate(event.target.value)} className="rounded-lg border border-slate-700 bg-slate-900 px-3 py-2 text-white" /><button type="button" disabled={reportLoading} onClick={() => void loadReport()} className="rounded-lg bg-emerald-600 px-4 py-2 font-bold disabled:opacity-60">{reportLoading ? t('ui.processing') : t('operations.reports.load')}</button></div>{report && <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{['totalAttendees', 'centerNetRevenue', 'teacherPayouts', 'digitalCollections'].map((key) => <Metric key={key} label={t(`operations.reports.${key}`)} value={key === 'totalAttendees' ? String(report[key]) : money(report[key] as number)} />)}</div>}<div className="max-w-xl rounded-xl border border-slate-800 bg-slate-900 p-5"><Field label={t('operations.reports.shiftId')} value={shiftId} onChange={setShiftId} /><button type="button" disabled={!shiftId || auditLoading} onClick={() => void loadAudit()} className="mt-3 rounded-lg border border-slate-700 px-4 py-2 font-bold disabled:opacity-60">{auditLoading ? t('ui.processing') : t('operations.reports.loadAudit')}</button>{auditLoading ? <div className="mt-4"><LoadingState /></div> : entries.length > 0 ? <div className="mt-4 space-y-2">{entries.map((entry, index) => <div key={`${String(entry.id || index)}`} className="rounded-lg border border-slate-800 p-3 text-sm"><span className="font-bold text-emerald-300">{String(entry.action)}</span><span className="ms-3 text-slate-400">{String(entry.createdAt)}</span>{entry.amount !== null && <span className="ms-3 text-amber-300">{money(entry.amount as number)}</span>}</div>)}</div> : shiftId && <div className="mt-4"><EmptyState message={t('operations.auditEmpty')} /></div>}</div></section>;
}

function Field({ label, value, onChange, type = 'text', required = true }: { label: string; value: string; onChange: (value: string) => void; type?: string; required?: boolean }) { return <label className="block text-sm font-semibold text-slate-300">{label}<input required={required} min={type === 'number' ? 0 : undefined} step={type === 'number' ? '0.01' : undefined} type={type} value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-3 text-white outline-none focus:border-emerald-500" /></label>; }
function Select({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: { value: string; label: string }[] }) { return <label className="block text-sm font-semibold text-slate-300">{label}<select required value={value} onChange={(event) => onChange(event.target.value)} className="mt-2 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-3 text-white outline-none focus:border-emerald-500"><option value="">{label}</option>{options.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select></label>; }
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-xl border border-slate-800 bg-slate-900 p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-xl font-bold text-white">{value}</p></div>; }
